plugins {
    id "org.ajoberstar.grgit" version "2.1.0"
}

allprojects {
    version = grgit.describe() + ((grgit.status().clean) ? "" : "-dirty")

    group = 'ch.mollusca'

    repositories {
        jcenter()
    }
}